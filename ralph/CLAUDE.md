# Ralph Agent Instructions

> **Ralph directory: `scripts/ralph/`** — All Ralph files live here. Your working directory is the repo root; always use the `scripts/ralph/` prefix for Ralph file operations.

## Step 0: Read State (ALWAYS FIRST)

1. Read `scripts/ralph/state.json`.
   - If it exists: orient from `currentStory`, `nextAction`, `branch`, `compactionNeeded`.
   - If missing: read `scripts/ralph/BOOTSTRAP.md` and follow it to create `scripts/ralph/state.json`, then continue.
2. If `compactionNeeded` is `true`: read `scripts/ralph/COMPACTION.md` and follow it. Do NOT work on stories. STOP after.
3. Proceed to **Your Task** below.

## Your Task

1. Check you're on the correct branch from `state.json`. If not, check it out or create from main.
2. Read `scripts/ralph/progress.md` in two reads: the `## Codebase Patterns` section from the top (~25 lines), then recent sessions from the bottom (last ~40 lines). Never read the entire file unless you are doing compaction.
3. Read `scripts/ralph/prd.json` and locate the story matching `state.json.currentStory` — read its acceptance criteria and description.
4. **If story has `iterationMode` field**: read `scripts/ralph/ITERATIVE.md` BEFORE starting work.
5. Implement that single user story.
6. Run quality checks (typecheck, lint, test; plus `validationCommand` if defined). Fix failures before committing. If unfixable: document in `scripts/ralph/progress.md`, do NOT set `passes: true`, end session.
7. **Frontend verification** (if UI): verify in browser using available tools. If none available, note in `scripts/ralph/progress.md`.
8. Update Institutional Memory (below) if you discover reusable conventions or gotchas. Optionally update project-level `CLAUDE.md` files for directory-scoped knowledge.
   - When Institutional Memory approaches budget (35+/40 lines): merge duplicate or overlapping entries into single combined lines.
   - Combine entries on the same topic into one comprehensive line.
   - Never remove entries unless proven wrong or fully subsumed by another entry.
9. **APPEND** to `scripts/ralph/progress.md`: date, story ID, what was done, files changed, and only genuinely reusable learnings.
    **PROMOTE (mandatory)**: re-read your learnings — any learning useful beyond the current story MUST go to `## Codebase Patterns` (top of `scripts/ralph/progress.md`). Cross-PRD conventions → Institutional Memory. Format: `scripts/ralph/PROGRESS-GUIDE.md`.
10. Update PRD: set `passes: true` for the completed story in `scripts/ralph/prd.json`. If final iteration, compact notes to max 2 lines (~200 chars). Details in `scripts/ralph/PROGRESS-GUIDE.md`.
11. **Update `scripts/ralph/state.json`** (see State Update Rules below).
12. Commit ALL story changes in a single commit after `progress.md`, `prd.json`, and `state.json` are final: `feat: [Story ID] - [Story Title]`
13. **STOP** — end response immediately. The shell loop detects completion from `scripts/ralph/prd.json`; a new session handles remaining work.

## State Update Rules

At END of every session, update `scripts/ralph/state.json`:

1. Scan `scripts/ralph/prd.json` for the next incomplete story (first with `passes: false`, ordered by `priority`)
2. `currentStory`: that story's ID (or `null` if all complete)
3. `storyTitle`: that story's title (or `"all stories complete"`)
4. `nextAction`:
   - If iterative with `currentIteration > 0`: `"iteration N/M (review pass)"` or `"iteration N/M"`
   - If iterative with `currentIteration == 0`: `"iteration 1/M"`
   - If standard: `"implement"`
   - If all complete: `"all stories complete"`
5. `branch`: keep as-is unless the PRD changes
6. `progressTokens`: estimate current size of `scripts/ralph/progress.md` (~15 tokens/line)
7. `compactionNeeded`: set to `true` if `progressTokens` exceeds `compactionThreshold`
8. `lastUpdated`: current date

## Critical Rules

These rules MUST be followed every session, even if earlier context is summarized.

1. **ALWAYS** read `scripts/ralph/state.json` FIRST, before any other file.
2. **ALWAYS** read `scripts/ralph/progress.md` (Codebase Patterns first) before starting work, but keep those reads bounded to the top pattern block and recent tail unless compacting.
3. **ALWAYS** work on exactly ONE story per session.
4. The story to work on comes from `state.json.currentStory` — NEVER scan `scripts/ralph/prd.json` to find it.
5. **ALWAYS** append to `scripts/ralph/progress.md` before ending a session. NEVER replace its contents (except during compaction — see Step 0.2).
6. **ALWAYS** commit all changes before ending a session. Commits MUST pass quality checks.
7. For iterative stories, all iteration-specific rules are in `scripts/ralph/ITERATIVE.md`.
8. After steps 9-13, **STOP**. End the session. A new session handles remaining work.
9. **ALWAYS** update `scripts/ralph/state.json` at the end of every session.
10. If `compactionNeeded` is true, follow `scripts/ralph/COMPACTION.md` — do NOT work on stories.

## Institutional Memory

> Format: `- **Name**: description` | Budget: 40 lines max
> Rules: Never remove unless proven wrong. No story-specific details or timestamps.

- **Bootstrap Session Access**: Treat `app.state.session_factory` from `sds.api.app` lifespan as the canonical session access point for API dependencies and integration tests.
- **Quality Gate Invocation**: Use explicit `.venv/bin/...` tool paths because bare commands are not guaranteed on PATH; the repo-wide gate is `make check`, the baseline test gate is `.venv/bin/pytest -q`, and repo-wide runs should not force `SDS_DATABASE_URL` because the offline Alembic SQL tests intentionally inject PostgreSQL URLs and the env override causes false SQLite failures.
- **ECHA Live Capture Contract**: Run live ECHA capture through headed Playwright Chromium with a Chrome-like user agent plus `navigator.webdriver` suppression, retry both search and substance page loads through transient 503 responses, and persist replayable `search|identity|classification` inbox artifacts that the existing importer path can replay offline.
- **Live Capture Storage Contract**: Reuse `sds.importers.capture_storage` for network-backed evidence capture flows; normalize/dedupe CAS-file targets once, persist `{source_name, scenario, captured_at, source_url, content_type, cas, language, checksum_sha256}` metadata beside raw response bodies, let importer-specific replay code derive `source_version` from the stored capture, and keep replay metadata checksums aligned with stored response bodies because checksum-gated public importers downgrade mismatches to `parser_failed`.
- **GUI Shell Gate**: Keep GUI-only auth in `sds.gui.auth` behind FastAPI `SessionMiddleware`, render pages through `sds.gui.rendering.render_gui_template`, mount packaged assets at `/gui/assets`, serve the pinned local HTMX runtime from `vendor/htmx-2.0.4.min.js`, and parse default URL-encoded login bodies directly so the server-rendered GUI stays dependency-light without `python-multipart`, a users table, or a frontend build pipeline; keep shared GUI auth/seeding helpers in `tests.support.gui` and drive manual browser review datasets through `sds.gui.review.render_gui_review_bundle` plus `scripts/render_gui_review_bundle.py`, following `docs/references/gui/review-workflow.md`, and when a review dataset depends on the default GUI generation path seed `mce_workbook` classification snapshots (or pass the matching source explicitly) so section 2 stays renderable.
- **GUI Form Gate**: Build reusable GUI form state through `sds.gui.forms.build_generation_form_context`, parse shared URL-encoded GUI bodies through `sds.gui.forms.parse_urlencoded_form_values`, render controls via `sds/gui/templates/_form_fields.html`, and keep explicit HTMX response/status targets plus per-field message slots so later GUI stories can add inline validation/results without duplicating field markup; successful HTMX submit flows should run through `sds.gui.generation.submit_gui_generation_request` and return `HX-Redirect` preview transitions instead of duplicating generation/render orchestration in the route.
- **CAS Registry Validation**: Centralize CAS normalization in `sds.importers.cas.normalize_cas`, preserving dash/spacing canonicalization while enforcing checksum validation before import or generation workflows continue.
- **Phrase Lookup Keying**: Keep phrase retrieval deterministic via `(code, language, source_version)`; canonicalize only `H|P|EUH` family prefixes and preserve suffix/composite code semantics with shared Pydantic validation before persistence.
- **Phrase Locale Decoupling**: Keep phrase locale validation in `sds.phrases.common.normalize_phrase_language` and workbook imports in `sds.phrases.excel` so phrase ingestion/stats can accept extra locales while API/CLI/GUI generation remains limited by `GenerationLanguage` (`sv|en`).
- **Phrase Seed Metrics**: Seed the canonical baseline file `data/phrases/phrases_sv_en_v1.csv` through `seed_phrase_library`, and preserve idempotency by reporting explicit `rows_read`, `rows_written`, and `rows_skipped` counts.
- **Phrase Import Audit Separation**: Record phrase ingestion provenance in dedicated `phrase_import_runs` rows linked optionally from `phrase_library.phrase_import_run_id`, keeping the linkage nullable so legacy seeded rows remain readable during phased backfills.
- **EUR-Lex Phrase Sourcing**: Resolve official CLP phrase content by fetching language-specific EUR-Lex landing pages only long enough to discover the linked `publications.europa.eu/.../DOC_2` XHTML documents, then derive `source_version` from the CELEX consolidation date and retain per-retrieval SHA-256 + source URL provenance.
- **Phrase Source Fallbacks**: Keep official and fallback phrase inputs on the shared `PhraseSourceFetchResult` contract; pin GitHub CSV sources to explicit refs, preserve retrieval checksums/URLs, and merge with fixed precedence `eurlex > github_csv > local` plus optional strict conflict failures.
- **Phrase Sync Auditing**: Orchestrate phrase refreshes through one sync service that creates one `phrase_import_runs` audit row per merged source input, derives its checksum from ordered retrieval metadata, counts only newly inserted rows in `records_processed`, and reports existing-library text conflicts without overwriting them unless strict mode explicitly aborts first.
- **Phrase Coverage Regression Gate**: Lock `data/phrases/phrases_sv_en_v1.csv` at 504 rows / 252 per language with `H=91`, `P=128`, `EUH=33`, and keep generation regressions for `H360FD`, `H360Fd`, `H350i`, `EUH201A`, `P301+P310`, and `P370+P380+P375[+P378]`.
- **MCE workbook Snapshot Reporting**: For snapshot imports, validate rows inside the `sds.importers.mce_classification` loop, report tolerant failures as `row_number|source_identifier|error_code|message`, wrap strict fail-fast imports in a savepoint, and still skip existing `(cas, snapshot_version, source_name)` rows before creating new import runs.
- **Substance Reference Snapshot Persistence**: Persist section 3 identity rows in `substance_reference_snapshots` keyed by `(cas, snapshot_version, source_name)`, keeping names, regulatory identifiers, and source-record metadata separate from classification snapshots.
- **MCE workbook Reference Field Import**: When workbook identity columns are present, persist `substance_reference_snapshots` from the same MCE workbook row/import run as the classification snapshot, but keep classification-key dedupe as the row skip decision.
- **CLP Label Parsing**: Parse classification labels through a dedicated parser interface with token extraction isolated from normalization so signal words, pictograms, and H/P/EUH code lists remain deterministic, keep fallback label completion in `sds.classification.label_elements.resolve_clp_label_elements`, allow deterministic H-code derivation only for primary ECHA snapshots, and persist per-field section 2 label origins in `sds_documents.section_source_trace.section_label_elements`.
- **SDS Draft Statusing**: Derive SDS `draft|review_required|ready` via explicit domain functions using classification snapshot presence plus phrase-text completeness at a fixed phrase source version.
- **SDS Document Persistence**: Persist generated drafts through a single service mapping into `sds_documents` so mode/version metadata, source summary, and JSON payload fields remain explicit and deterministic before repository writes.
- **SDS Payload Determinism**: Keep surrogate database IDs out of canonical SDS JSON payloads, persist reviewer-only per-section provenance and diagnostics such as `import_run_id` and `section_diagnostics` in storage/internal fields like `sds_documents.section_source_trace`, build those diagnostics separately from section text rendering, keep section `16` reader-facing by leaving source-history out of canonical rendering, and surface reviewer context only in read-model layers such as `sds.gui.source_explanations` and GUI history expansions.
- **SDS Output Payload Modeling**: Serialize canonical SDS payloads through `sds.generation.output_models.SdsOutputPayload` with numeric section-key aliases, top-level `document_control|provenance|validation`, and section 1 `product_identifiers`; reuse section 1 supplier schemas via aliases instead of duplicating near-identical Pydantic models or `jscpd` will fail.
- **SDS API Error Shape**: Map invalid CAS and missing SDS document conditions to stable API payloads shaped as `{error:{code,message}}` with explicit `invalid_cas` and `sds_not_found` codes.
- **SDS Entry Validation Reuse**: Reuse `GenerateSdsDraftCommand` at API, Typer, and service entrypoints so `cas` stays required, `product_number` stays optional, `language` defaults to `sv`, and request validation remains centralized.
- **Generation Effective Date Clocking**: Resolve `document_control.effective_date` through an injected callable in generation services so deterministic tests can pin dates without exposing `effective_date` as external input.
- **Generation Response Alignment**: Serialize CLI and `POST /sds/generate` success payloads through `sds.generation.presentation.build_generate_sds_operation_response`, preserving `sds_id`/`status` while keeping source metadata and missing-phrase counts in sync.
- **Renderable SDS Contract**: Build PDF inputs from stored drafts through `sds.rendering.contract.build_renderable_sds_document`, keep canonical localized headings in `sds.rendering.section_titles` for both contract and renderer outputs, require `document_control.revision_number` plus section 1 `product_identifiers.product_number`, normalize section 2 pictogram codes into ordered logical asset keys before any template/layout logic runs, render final PDFs through `sds.rendering.pdf.render_sds_pdf` with a numbered-canvas pass that writes final page counts into the title metadata row and footer, load versioned Chemtronica SVG assets plus localized page specs only from `sds.rendering.assets` and `sds.rendering.layout` under `sds/rendering/assets/<template_version>/`, exclude duplicated packaged SVG pictograms in `.jscpd.json` so the zero-threshold duplicate gate targets authored source rather than mirrored shipped assets, and gate layout-sensitive changes through `sds.rendering.review` plus the committed `tests/fixtures/pdf_review/...` metadata/payload/golden workflow with `scripts/render_pdf_review_fixture.py` and MCP Playwright review of the generated bundle.
- **PDF Artifact Storage**: Persist final PDF lifecycle state in `sds_pdf_artifacts` linked to `sds_documents`, copy successful files under `Settings.artifact_storage_dir/sds-{sds_id}/<sha256>.pdf`, route stored-document renders through `sds.rendering.service.render_stored_sds_pdf`, route CAS+product-number final-PDF delivery through `sds.rendering.service.generate_final_sds_pdf`, and for GUI preview/download flows resolve metadata through `sds.gui.preview.get_gui_artifact_preview`, stream bytes via `sds.gui.preview.build_gui_artifact_file_response`, expose protected `/gui/artifacts/{artifact_id}/file|download` routes, and keep handoff filenames deterministic as `sds-{product_number}-{language}-{sds_id}.pdf` so API, CLI, and GUI reuse the same stored artifact without re-rendering or mutating the stored SDS payload.
- **GUI Artifact Read Models**: Keep preview/history queries in `sds.gui.artifact_views`, deriving GUI-visible rows from joined `sds_pdf_artifacts` + `sds_documents`, storing reviewer workflow state on `sds_pdf_artifacts.review_state` rather than `sds_documents.status`, projecting latest reviewer edits from append-only `sds_manual_overrides` rows into the effective preview payload without mutating stored artifacts or `sds_documents.sds_payload`, calculating `quality_score` / `quality_band` from persisted `sds_payload.validation` plus active override count through `sds.review.quality.assess_draft_quality`, validating history batch actions only against the current `list_recent_gui_visible_pdf_artifacts` row set, requiring linked `sds_documents.status == "ready"` before marking an artifact `publishable`, skipping missing files or section 1 product numbers, and applying the latest-25 limit only after invalid rows are filtered.
- **Section 1 Internal Defaults**: Build section 1 supplier identity, identified use, and Sweden emergency fields from `data/internal/chemtronica_supplier_defaults.yaml` via one validated mapper, and keep legacy `product_identity` and `supplier` keys present so external SDS data never becomes the source of truth.
- **Internal Product Catalog Identity**: Persist Chemtronica catalog rows in `internal_product_catalog` through one repository that normalizes `product_number`, canonicalizes CAS with `normalize_cas`, preserves exact `product_name`, keeps one logical row per `product_number`, stores optional workbook metadata (`datasheet_url`, `msds_url`, `storage_temperature`, `shipping`) on that row, links workbook-driven persistence to dedicated `product_catalog_import_runs` through nullable `import_run_id`, centralizes `Product List` preamble/header validation, row normalization, identity extraction, and `Catalog_No` grouping in shared `sds.products.mce_workbook` helpers, hashes workbook bytes for import-run checksums, counts only persisted catalog rows in `records_processed`, persists per-run readiness snapshots (`distinct_cas_count`, missing required-field counts, quarantine reason counts, auxiliary sheet presence) on the import run, quarantines any grouped catalog with missing, invalid, or conflicting identity values instead of inferring them, and treats active-only lookup as the section 1 identity source.
- **Product Catalog Validation**: Validate optional generation `product_number` through one generation-domain lookup before draft assembly; expose `product_not_found` and `product_cas_mismatch` only for catalog lookup failures, and treat blank catalog names as generic domain validation.
- **Targeted Evidence Hydration Selection**: Resolve hydration work only from explicit CAS or catalog subset files, expand catalog products to deduped CAS targets before applying any limit, use existing `external_source_evidence` rows as the resumable skip signal so manual hydration stays bounded and never degrades into an implicit full-catalog fetch, and keep missing catalog numbers as actionable selection gaps so closure iterations cannot report a false complete pass.
- **Evidence Gap Closure Contract**: Treat section 4-15 closure as effective section coverage: mixed states stay actionable until one tier fully covers the section or the official/supplier pair explicitly resolves to `no_public_coverage` plus `no_supplier_coverage`, and once one tier fully covers every subsection, targeted hydration should stop retrying the opposite tier for that section; live closure should start with a `--cas-file` audit, group public captures by required official section tuples, run supplier capture only for CAS needing supplier retry, pass section-scoped source-acquisition outcomes into targeted hydration, and persist status-only `parser_failed` / `no_supplier_coverage` evidence rows when requested artifacts are absent so final diagnostics do not collapse those failures into generic `missing`.
- **Golden Set Validation Contract**: Build golden-set batches by running `sds.evidence.closure.run_live_evidence_closure` for the selected CAS file, then generate one draft per audited CAS and reuse `sds.evidence.golden_set.validate_golden_set_payload` so CLI/reporting surfaces share the same completeness checks for audit failures, placeholder sections, missing label elements, missing section `3` identity, missing phrase codes, and missing section provenance across sections `2-15`; when reviewer triage needs the same drill-down as the GUI, serialize `draft.section_source_trace.section_diagnostics` through `serialize_golden_set_section_diagnostics` so each case carries deterministic section `2-16` source/subsection outcomes without changing the top-level completeness fields.
- **Golden Set Preflight Contract**: Build preflight ranking in `sds.products.preflight` over one product-catalog import run’s grouped CAS targets plus the latest MCE classification/reference snapshots, latest ECHA search evidence rows, latest official section rows, and latest supplier discovery rows, keep CLI formatting in `sds.cli`, and only disqualify candidates for irreducible section `2`/`3` gaps, audit `conflict|parser_failed`, or explicit public no-coverage sections without an allowlisted supplier candidate.
- **Bootstrap DB Orchestration**: Bootstrap fresh databases through one declarative flow that sequences local phrase sync, MCE workbook import, Swedish OEL imports over the just-imported MCE workbook CAS set from one captured AFS 2023:14 snapshot directory, then product catalog import; bootstrap v1 must not prefetch section 4-15 external evidence.
- **SDS Coverage Mapping Contract**: Load subsection coverage from `data/sds_coverage/coverage_map_v1.yaml` via typed `sds.generation.coverage` models, keep keys aligned to `docs/references/templates/Mall.xlsx`, and express section 1 identity/emergency provenance through explicit `field_sources`.
- **Generation Version Fallback**: Resolve implicit latest snapshot/phrase versions through shared natural-sort helpers so numeric `v` suffixes and embedded date segments outrank plain lexical ordering.
- **External Evidence Persistence**: Persist official and supplier captures in `external_source_evidence` keyed by `(cas, source_tier, source_name, source_version, checksum_sha256)`, link them to `import_runs`, keep common provenance fields explicit, and store source-specific metadata plus normalized extracted content in JSON columns.
- **ECHA Evidence Role Versioning**: When one observed ECHA snapshot checksum is reused for both identity and C&L extracts, suffix evidence `source_version` with `:identity` and `:classification` so role-specific rows stay insertable under `external_source_evidence` uniqueness while preserving identical local snapshot identity metadata.
- **Section 2/3 Snapshot Precedence**: In generation, treat canonical ECHA `substance-information/-/substanceinfo/...` URLs as the only primary ECHA inputs; resolve section 2 from ECHA when usable with MCE workbook fallback for missing phrase-code coverage, resolve section 3 identity with ECHA-first/MCE workbook fallback, and record non-primary or legacy ECHA fallbacks via provenance placeholders.
- **Evidence Section Resolver Precedence**: In generation, resolve evidence-backed sections from the latest `external_source_evidence` rows per tier, merge subsection coverage official-first with supplier fallback only for empty subsections, emit explicit `[Review required]` subsection warnings when gaps remain after fallback, treat section-specific non-empty official/supplier disagreements as review-required conflicts while retaining the official text, keep placeholder text only while no section-specific evidence rows exist, build adjacent text-section resolvers through `sds.generation.evidence_text_sections.build_mapped_evidence_text_section_resolver` when subsection payloads are plain scalar/list fields, keep section-local `resolve_evidence_text_section` formatters when subsection rows need structured tags, treat explicit-empty official optional lists such as `5.4 additional_information`, `6.4 reference_to_other_sections`, and `9.2 other_information` as complete only through section-local `subsection_resolver` logic instead of weakening the shared resolver, for section 7 render subsection `7.2` from `storage_conditions` followed by `incompatibility_notes`, and when both official and supplier `7.3` states are explicit no-coverage render the language-aware builder-owned default sentence while keeping section provenance on the consulted evidence rows, for section 9 resolve subsection `9.1` per physical property so official values/units win while supplier evidence fills only explicit null gaps, render unresolved properties as inline review-required bullets, treat explicit empty `9.2` `other_information` lists as allowlisted complete evidence while missing `9.2` evidence still requires review, and let golden-set section `9` review checks follow the same diagnostics-backed completeness rule, for section 10 resolve `10.1-10.6` only from direct section 10 evidence payloads so supplier fallback fills uncovered subsections without inferring stability/reactivity text from section 9 properties, for section 13 render subsection `13.1` by concatenating `disposal_constraints` then `waste_handling_notes`, keeping official disposal evidence when present and using supplier fallback only when the subsection is otherwise empty, for section 14 resolve transport subsections individually so official scalar/list values win while supplier fallback fills only empty transport subsections and missing transport coverage remains explicit review-required warnings, and for section 15 render subsection `15.1` as `[source_family] reference` lines and subsection `15.2` as `[source_family] list_name: entry` lines so official regulatory evidence stays tagged while supplier fallback fills only empty subsection coverage.
- **Mapped Text Section Resolver Reuse**: For evidence-backed text sections whose subsections each map to one or more scalar or ordered list fields, build resolver wrappers through `sds.generation.evidence_text_sections.build_mapped_evidence_text_section_resolver` and `sds.generation.evidence_section_helpers.subsection_field_lines` so new section modules stay isolated without tripping the zero-threshold `jscpd` gate.
- **Swedish OEL Source Separation**: For section 8 imports, persist Arbetsmiljoverket OEL snapshots in `swedish_occupational_exposure_limits` with explicit `no_swedish_oel_coverage` rows when no hit exists, and do not map foreign/supplementary exposure concepts into Swedish OEL authority fields.
- **Section 8 Public Coverage Mapping**: For section 8 public-source evidence, parse AFS 2023:14 table rows by requested CAS (shared snapshot may contain many CAS values), persist subsection `8.1` control parameters with explicit `swedish_oel_status`, and keep subsection `8.2` as explicit no-coverage placeholders unless deterministic supplementary mapping exists.
- **Public Section Import Layering**: Implement section 4/5/6/7/9/10/11/12/13/14 public-source importers as section-specific payload mappers over shared `sds.importers.public_evidence` scaffolding (`resolve_public_section_snapshot`, `run_public_section_import`, shared command/result schemas), reuse `normalize_html_text` for HTML-cell cleanup, classify section 6 `SPILLAGE DISPOSAL` sentences into personal/environmental/cleanup buckets, keep section 7 subsection-level coverage statuses explicit (`7.1`/`7.2` covered vs `7.3` no coverage), for section 9 persist physical-property fields as `{value, unit}` objects with explicit null placeholders for missing properties, for section 10 map only `Physical dangers`/`Chemical dangers` evidence into explicit subsection payloads without inferring from section 9 properties, for section 11 preserve partial endpoint extraction by keeping `11.1` toxicological-effects and `11.2` potential-health-effects coverage statuses explicit, for section 12 map `ENVIRONMENT` evidence to `12.1` ecology plus keyword-driven `12.2` degradability while leaving `12.3-12.7` explicit no-coverage placeholders, for section 13 map `SPILLAGE DISPOSAL` evidence to `13.1` `disposal_constraints` plus `waste_handling_notes` without synthesizing instructions on no-coverage outcomes, and for section 14 map explicit ICSC transport markers into `14.1` UN number, `14.3` hazard class, `14.4` packing group, and `14.6` transport notes while preserving `14.2`/`14.5`/`14.7` no-coverage placeholders when transport data is absent, and for section 15 map regulatory evidence into explicit `15.1` source-family-tagged references plus `15.2` list-membership entries while treating CAS-absent regulatory-source snapshots as explicit no-coverage outcomes.
- **Supplier SDS Discovery Traceability**: Discover supplier candidates with strict query order (`cas` first, then `product_name` fallback only when CAS yields none), persist one `external_source_evidence` row per approved source/query stage with explicit parser status (`candidates_found|no_candidate|parser_failed`), keep discovery outputs as candidate metadata for downstream ranking/extraction, and for explicit `MERCK / Sigma-Aldrich` support parse `sigmaaldrich.com` `srp-substance-group` rows through `NAME-pdp-link-*` plus `sds-*` hooks so English replayable `/sds/{brand}/{product_identifier}` PDFs are derived deterministically with `merck_sigmaaldrich` metadata.
- **Supplier SDS Ranking Determinism**: Rank supplier SDS candidates only after approved vendor+domain allowlist checks (`fluorochem.co.uk`, `scbt.com`, `sigmaaldrich.com`) using deterministic precedence `exact_cas > has_sds_document > language (sv/en) > revision_date > completeness > confidence > lexical_url`, and emit explicit `critical_field_conflict` when top-ranked candidates disagree on critical fields.
- **Supplier SDS Section Fallback Parsing**: Feed approved supplier SDS PDFs into section importers as layout-preserving extracted text, reuse `sds.importers.supplier_sds_sections` for section slicing/labeled-row parsing plus import-run/evidence persistence, store fallback precedence and extraction confidence in metadata, keep supplier section payloads schema-aligned with the corresponding public-source imports, stop subsection scans at the next available supplier heading when intermediate headings are missing, route `See Section ...` cross-references into explicit reference subsections instead of leaving them in precaution text, for section 7 isolate handling/storage subblocks before label parsing so headings do not bleed across rows while unreadable OCR fragments are skipped and `subsection_7_1|7_2|7_3` statuses stay explicit, for section 10 map only direct stability/reactivity labels while ignoring unrelated physical-property rows so fallback coverage stays non-inferential, for section 11 trim supplier ingredient tables out of likely-route blocks before label parsing while mapping `ATEmix` rows into short-term toxicology and preserving subsection-level no-coverage states when only routes or chronic effects are present, for section 12 parse direct ecological labels from section tables while ignoring embedded aquatic-toxicity grids so subsection `12.1`-`12.7` statuses stay explicit and `No information available` rows remain `no_supplier_coverage`, for section 13 drive disposal fallback only from labeled tabular rows via shared tabular-section/sentence-note helpers, mapping `Disposal of wastes` plus `Other Information` into `disposal_constraints`, `Contaminated packaging` into `waste_handling_notes`, and unlabeled prose to explicit `no_supplier_coverage`, and for section 15 scope supplier inventory and right-to-know parsing to named section subblocks before regex extraction so table statuses do not bleed across regulatory tables while shared section 15 payload builders keep public and fallback subsection shapes aligned.
