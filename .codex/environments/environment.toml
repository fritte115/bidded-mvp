# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "Bidded"

[setup]
script = '''
#!/usr/bin/env bash
set -euo pipefail

WORKTREE_ROOT="$PWD"
REPO_ROOT="$WORKTREE_ROOT"
SCRIPT_PATH="$REPO_ROOT/scripts/worktree_env_setup.sh"

cd "$REPO_ROOT"
if [ -f "$SCRIPT_PATH" ]; then
  WORKTREE_ROOT="$WORKTREE_ROOT" bash "$SCRIPT_PATH"
else
  echo "Skipping optional worktree setup; missing $SCRIPT_PATH" >&2
fi

'''

[cleanup]
script = '''
#!/usr/bin/env bash
set -euo pipefail

WORKTREE_ROOT="$PWD"
REPO_ROOT="$WORKTREE_ROOT"
SCRIPT_PATH="$REPO_ROOT/scripts/worktree_env_cleanup.sh"

cd "$REPO_ROOT"
if [ -f "$SCRIPT_PATH" ]; then
  WORKTREE_ROOT="$WORKTREE_ROOT" bash "$SCRIPT_PATH"
else
  echo "Skipping optional worktree cleanup; missing $SCRIPT_PATH" >&2
fi
'''
